<UserControl
    x:Class="GenshinLyreMidiPlayer.WPF.Views.PianoSheetView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d"
    xmlns:modern="http://schemas.modernwpf.com/2019"
    xmlns:s="https://github.com/canton7/Stylet"
    xmlns:viewModels="clr-namespace:GenshinLyreMidiPlayer.WPF.ViewModels"
    xmlns:core="clr-namespace:GenshinLyreMidiPlayer.WPF.Core"
    xmlns:properties="clr-namespace:GenshinLyreMidiPlayer.Data.Properties;assembly=GenshinLyreMidiPlayer.Data"
    d:DataContext="{d:DesignInstance Type=viewModels:PianoSheetViewModel}">

    <UserControl.Resources>
        <properties:Settings x:Key="Settings" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- ── Page header ────────────────────────────────── -->
        <StackPanel Grid.Row="0" Margin="0,0,0,18">
            <TextBlock Text="TOKEN SHEET" Style="{StaticResource EyebrowStyle}" />
            <TextBlock Text="Sheet" Style="{StaticResource PageTitleStyle}" />
        </StackPanel>

        <!-- ── Controls card ──────────────────────────────── -->
        <Border Grid.Row="1" Style="{StaticResource WinPanel}" Padding="18,14" Margin="0,0,0,16">
            <StackPanel>
                <!-- Current song -->
                <TextBlock Foreground="{StaticResource Ink2Brush}" FontSize="12.5" Margin="0,0,0,14">
                    <Run Text="Song: " Foreground="{StaticResource Ink4Brush}" />
                    <Run Text="{Binding PlaylistView.OpenedFile.Title, Mode=OneWay, FallbackValue='Nothing loaded'}" />
                </TextBlock>

                <!-- Row 1: Delimiter + Layout -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <StackPanel Margin="0,0,16,0">
                        <TextBlock Text="Delimiter" Style="{StaticResource EyebrowStyle}" Margin="0,0,0,6" />
                        <TextBox Text="{Binding Delimiter, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource WinTextBox}" Width="80" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="Layout" Style="{StaticResource EyebrowStyle}" Margin="0,0,0,6" />
                        <ComboBox ItemsSource="{x:Static core:Keyboard.LayoutNames}"
                                  SelectedItem="{Binding SelectedLayout}"
                                  SelectedIndex="{Binding Default.SelectedLayout, Source={StaticResource Settings}}"
                                  DisplayMemberPath="Value" MinWidth="140" />
                    </StackPanel>
                </StackPanel>

                <!-- Row 2: Split size numbers -->
                <StackPanel Orientation="Horizontal">
                    <StackPanel Margin="0,0,12,0">
                        <TextBlock Text="Bars" Style="{StaticResource EyebrowStyle}" Margin="0,0,0,6" />
                        <modern:NumberBox Value="{Binding Bars}" Width="80" />
                    </StackPanel>
                    <StackPanel Margin="0,0,12,0">
                        <TextBlock Text="Beats" Style="{StaticResource EyebrowStyle}" Margin="0,0,0,6" />
                        <modern:NumberBox Value="{Binding Beats}" Width="80" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="Shorten every" Style="{StaticResource EyebrowStyle}" Margin="0,0,0,6" />
                        <modern:NumberBox Value="{Binding Shorten}" Minimum="1" Width="100"
                                          ToolTip="Divides the delimiter by this number" />
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- ── Result text ─────────────────────────────────── -->
        <Border Grid.Row="2" Style="{StaticResource WinPanel}" Padding="0">
            <TextBox Text="{Binding Result, Mode=OneWay}"
                     IsReadOnly="True"
                     Style="{StaticResource WinTextBoxMulti}"
                     Background="Transparent"
                     BorderThickness="0"
                     Padding="16"
                     FontFamily="{StaticResource MonoFont}"
                     FontSize="13" />
        </Border>

        <!-- ── Action buttons ──────────────────────────────── -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,12,0,0">
            <Button Content="Copy to Clipboard"
                    Style="{StaticResource AccentBtn}"
                    Command="{s:Action CopyToClipboard}"
                    Height="36" Padding="16,0" Margin="0,0,8,0" />
            <Button Content="Refresh"
                    Style="{StaticResource WinBtn}"
                    Command="{s:Action Update}"
                    Height="36" Padding="16,0" />
        </StackPanel>
    </Grid>
</UserControl>
